# Проверка 8 сентября 2026

Актуальная итерация: полотно вокруг работ, hero без сжатия, синяя точка, капс профессии и живые акценты в опыте/контактах.

- TypeScript/Vite build проходит.
- npm test: 11/11, включая физику интро, контент, импорт и конфликты редактора.
- Lint: без ошибок, пять прежних предупреждений в Typed, Cursor, Lockup и Symbiote.
- Контур Mark, физика introSim и глобальная палитра не изменены. В Intro и Lockup заменён только класс оформления точки: синий sky и лёгкая тень.
- Публикация, GitHub, данные и редактор не менялись.

## Браузер

Проверки выполнены через CUA во встроенном Chromium.

- После reload увидено промежуточное «ГР», затем полная профессия капсом. Печать запускается.
- При scrollY около 369 px у hero transform:none; у canvas matrix(1,0,0,1,1134.88,0), у знака единичный масштаб. Непропорционального сжатия нет.
- Просмотрены пролёт возле заголовка, обёртывание первых обложек и переход к Мультиварке/YoToys. После исправления отрисовки цельные края и градиентная поверхность подтверждены повторной загрузкой.
- В опыте и контактах лента отсутствует; отдельные кольца видны. Контактный экран раскрывается, почта и ссылки читаются.
- Ошибок в консоли при просмотре не обнаружено.
- На ширине 390 px проверены hero и контакты: scrollWidth = clientWidth, письмо и декоративная фигура помещаются. Временный viewport сброшен.
- Градиент hero подтверждён визуально. Hero, шапка и катящийся шарик интро имеют одинаковый синий rgb(47,97,130); интро проверено кнопкой повтора.
- Ранее в этой итерации проверены кнопка копирования (сообщение «Адрес скопирован»), точные href резюме и mailto, переход на контакт и обновление с возвратом на hero.

## Ограничения

Это моделирование объёма на Canvas и CSS 3D, без WebGL и физической симуляции ткани. Safari, Firefox и реальные телефоны не запускались. Reduced motion проверен по коду и CSS, без смены системной настройки.

## Контактное приглашение — следующая итерация

- Build проходит; lint без ошибок, с пятью прежними предупреждениями.
- При ширине 563 px просмотрен финальный экран: явная светлая кнопка, орбита и синяя точка, сохранены почта/копирование/контакты. Переполнения нет.
- При указателе слева от кнопки data-near=true и смещение поверхности −9 px; область ссылки остаётся на месте.
- Shift+Tab от копирования приводит на «Написать Сулейману о проекте», видимая outline solid. Точный href mailto проверен без запуска почтового приложения.
- На ширине 390 px кнопка и адрес помещаются; scrollWidth = clientWidth. Временный viewport сброшен.

## 9 сентября — полотно и печать в контактах

- Финальная реализация заменяет орбиты на ContactCloth; в опыте отображается Mark с синей точкой. В DOM нет прежних orbit-glyph/contact-loop.
- Build проходит, lint без ошибок (пять прежних предупреждений).
- Через CUA при переходе из hero прочитана пустая contact-type-ink, позднее — полная фраза. Задержка 500 мс и отмена таймера при уходе проверены по коду; инструментально точное время не измерялось.
- Проверены начало/конец и обратный ход scroll progress. Закрепление работает после замены нижнего padding на min-height контейнера; область ссылки неподвижна во время сцены.
- Финальное полотно просмотрено в браузере. На ширине 390 px scrollWidth = clientWidth = 367 px (с учётом полосы прокрутки), canvas и кнопка помещаются. Временный viewport сброшен.
- Shift+Tab от адреса приводит на «Написать Сулейману о проекте» с видимым фокусом. Проверен точный mailto без запуска почтового приложения.
- Safari/Firefox и реальные устройства не проверялись. Reduced motion проверен по коду/CSS, без смены системных настроек.

## Описания кейсов — 9 сентября

- Все кейсы используют CaseCopy с тремя разделами и явно обозначенной рыбой при пустых полях. Настоящие тексты вводятся через новые поля редактора; краткий note сохранён.
- Build проходит; 12/12 тестов, включая новый тест необязательного описания, абзацев и неверных типов полей. Lint без ошибок, пять прежних предупреждений.
- CUA: Beauty Lab просмотрен на широком экране, YoToys на 390 px — одноколоночный текст 16 px, scrollWidth = clientWidth = 367 px. Viewport восстановлен.
- У Мультиварки проверен порядок: два вертикальных макета, описание деталей, ещё два макета. В трёх проверенных кейсах присутствуют все три раздела. Повторяющихся id нет; переход к описанию работает.
- Авторизация и публикация в редакторе не выполнялись. Проверены схема/валидация и привязка полей по коду; сохранение в удалённый репозиторий не запускалось.

## Пропорции устройств и стыки ткани — 9 сентября

- Build проходит, 12/12 тестов проходят. Lint: ошибок нет, те же пять предупреждений в прежних компонентах. git diff --check без ошибок пробелов.
- CUA/Chromium: просмотрены оба ноутбука в начале подборки, YoToys, Мультиварка и Оплата картой. Клавиатура и трекпад находятся на основании полной ширины; экран смартфона почти фронтальный. Проверены промежуточный поворот и обратная прокрутка. Все пять экранов загружены.
- После последнего изменения ткани повторно просмотрен переход между Beauty Lab и Василем: прежний плоский торец внутри соседней карточки сменился сужением к ребру. При переходе переднего/заднего прохода нет прежней смены градиента. Защита подписей теперь также с плавным сужением.
- Нажатие YoToys открывает #/work/yotoys, исходные изображения загружены; «Все работы» возвращает в подборку. В консоли ошибок не обнаружено.
- На эффективной ширине окна 390 CSS px проверены размеры DOM: scrollWidth = clientWidth = 367 px (полоса прокрутки занимает остальное), карточки помещаются. Захват скриншота при временном viewport во встроенном браузере отображал смещённый фрагмент, поэтому полная визуальная проверка узкого экрана ограничена. Viewport восстановлен; широкий предпросмотр проверен повторно.
- Reduced motion проверен по коду/CSS. Safari, Firefox и физические устройства не проверялись. Размеры корпуса основаны на спецификациях Apple, мелкие детали моделей — визуальная реконструкция. Публикация не выполнялась.
